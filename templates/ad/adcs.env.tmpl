# GENERATED by bootstrap.py from templates/ad/adcs.env.tmpl - do not edit this
# copy; edit config.env and re-run ./bootstrap.py.
#
# Site values for the AD CS PowerShell in scripts/ad. Copy this file to the
# Windows host alongside the .ps1 files - Get-AdcsConfig.ps1 reads it from its
# own directory by default, or from $env:ADCS_ENV_FILE / -ConfigFile.
#
# Same KEY=VALUE format as config.env. Everything site-specific lives here, so
# the .ps1 files stay generic and can be edited and linted as ordinary
# PowerShell rather than as Jinja templates.

# ------------------------------------------------------------------ directory
AD_NETBIOS={{ AD_NETBIOS | required("AD_NETBIOS") }}
AD_GROUP_BASE_DN={{ AD_GROUP_BASE_DN | required("AD_GROUP_BASE_DN") }}
AD_SERVICE_ACCOUNT_DN={{ AD_SERVICE_ACCOUNT_DN | required("AD_SERVICE_ACCOUNT_DN") }}

# ---------------------------------------------------------------------- AD CS
ADCS_CA_NAME={{ ADCS_CA_NAME | required("ADCS_CA_NAME") }}
ADCS_HOST={{ ADCS_HOST | required("ADCS_HOST") }}
ADCS_TEMPLATE={{ ADCS_TEMPLATE | required("ADCS_TEMPLATE") }}
# The account cert-manager authenticates to /certsrv as. It needs Enroll on
# ADCS_TEMPLATE - New-AdcsEnrollmentAccount.ps1 creates it and grants that.
ADCS_ENROLL_USER={{ ADCS_ENROLL_USER | required("ADCS_ENROLL_USER") }}
ADCS_URL={{ ADCS_URL | required("ADCS_URL") }}

# Web enrollment (/certsrv) application pool gMSA
ADCS_WEB_GMSA_NAME={{ ADCS_WEB_GMSA_NAME | required("ADCS_WEB_GMSA_NAME") }}
ADCS_WEB_GROUP_NAME={{ ADCS_WEB_GROUP_NAME | required("ADCS_WEB_GROUP_NAME") }}

# Certificate Enrollment Web Service (CES) gMSA - only New-AdcsCesGmsa.ps1
CES_GMSA_NAME={{ CES_GMSA_NAME | required("CES_GMSA_NAME") }}
CES_GROUP_NAME={{ CES_GROUP_NAME | required("CES_GROUP_NAME") }}
CES_HOST={{ CES_HOST | required("CES_HOST") }}

# -------------------------------------------------------------------- Proxmox
# Used to snapshot the DC before the CA role is installed. The token SECRET is
# deliberately not here - set $env:PVE_API_TOKEN or let the script prompt.
PVE_API_HOST={{ PVE_API_HOST | required("PVE_API_HOST") }}
PVE_API_TOKEN_ID={{ PVE_API_TOKEN_ID | required("PVE_API_TOKEN_ID") }}
PVE_DC_VMID={{ PVE_DC_VMID | required("PVE_DC_VMID") }}
PVE_NODE={{ PVE_NODE | required("PVE_NODE") }}
